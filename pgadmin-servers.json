{
    "Servers": {
      "1": {
        "Name": "Postgres Database",
        "Group": "Servers",
        "Host": "${POSTGRES_HOST}",
        "Port": "${POSTGRES_PORT}",
        "MaintenanceDB": "postgres",
        "Username": "${POSTGRES_USER}",
        "Password": "${POSTGRES_PASSWORD}",
        "SSLMode": "prefer"
      }
    }
  }